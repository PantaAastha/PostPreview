# MCP Server Configuration
PORT=3000

# Widget Base URL - must be the HTTPS ngrok URL for the ui tunnel
# When running ngrok: ngrok start --all --config=path/to/ngrok.yml
# Get the HTTPS URL for the "ui" tunnel and set it here
WIDGET_BASE_URL=https://your-ngrok-ui-url.ngrok-free.app
